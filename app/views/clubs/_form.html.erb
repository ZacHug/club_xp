 <%= simple_form_for [club, comment],remote: true, defaults: { input_html: { class: 'flex-form'} } do |f| %>
    <%= f.input :content, placeholder: 'leave a comment...', label:false %>
    <%= button_tag( :class => "btn btn-primary cmt-btn") do %>
      <i class="fas fa-paper-plane"></i>
      <% end %>
    <% end %>
